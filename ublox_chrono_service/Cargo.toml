[package]
name = "ublox_chrono_service"
version = "0.1.0"
edition = "2021"

[dependencies]
nix = { version = "0.29.0", features = ["signal", "event", "term", "mman"] }
log = "0.4"
env_logger = "0.11.6"
serial_port = { path = "../serial_port" }
